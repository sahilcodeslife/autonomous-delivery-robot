#must do all the commits from inside the container. 

# Check status
git status

# Add all changes
git add .

# Commit with message
git commit -m "Add ROS2 setup scripts and package configuration"

# Push to remote
git push origin main

